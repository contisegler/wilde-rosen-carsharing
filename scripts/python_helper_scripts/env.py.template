"""
Environment configuration for Firebase projects.
This file contains project IDs for different environments.

SETUP INSTRUCTIONS:
1. Copy this file to env.py
2. Replace the placeholder project IDs with your actual Firebase project IDs
3. The env.py file will be git-ignored for security

Note: Staging uses the same Firestore as production.
"""

# Firebase Project IDs for different environments
PROJECT_IDS = {
    "npr": "your-npr-project-id",      # Development/NPR environment
    "prd": "your-production-project-id" # Production environment
}

# Individual project ID variables for convenience
NPR_PROJECT_ID = PROJECT_IDS["npr"]
PRD_PROJECT_ID = PROJECT_IDS["prd"]
